// import { PrismaAdapter } from "@auth/prisma-adapter";
// import { prisma } from "./lib/prisma";

// export default customAdapter = (client, options = {}) => {
//   return {
//     ...PrismaAdapter(prisma),
//     ...options,
//   };
// };
